<h1>THIS IS A CONTACT'S PROFILE PAGE</h1>
<%= link_to "BACK TO FEED", newsfeed_path %>

<h2><%= @contact.name %></h2>
<ul>

  <% @messages.each do |message| %>
    <li><%= message.created_at %></li>
    <% # do we want html %>
    <li><%= message.body_html.html_safe %></li>
  <% end %>
</ul>
